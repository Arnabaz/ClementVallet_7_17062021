// --- IMPORTS ---
// Importation des différents modules
const express = require("express");
const db = require("./config/db.config");

// --- APP ---
const app = express();

// --- EXPORT ---
module.exports = app;